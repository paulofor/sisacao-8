# URL base da API utilizada pelo frontend.
# Padrão recomendado: vazio (mesmo host do frontend).
VITE_API_BASE_URL=
# Para ambiente local separado, pode usar:
# VITE_API_BASE_URL=http://localhost:8080
# Para proxy em /api, use:
# VITE_API_BASE_URL=/api
