# URL base da API utilizada pelo frontend.
# Ajuste para o endpoint exposto pelo backend que consulta o BigQuery.
# Em produção, o padrão do app é base vazia (mesma origem).
VITE_API_BASE_URL=http://localhost:8080
# Para produção na mesma origem (ex.: Nginx servindo frontend e backend), use vazio:
# VITE_API_BASE_URL=
# Para produção com proxy /api, use:
# VITE_API_BASE_URL=/api
