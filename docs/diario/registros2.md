# Diário de registros do projeto — parte 2

## 2026-07-10 — Rotação dos arquivos de acompanhamento
- Atualizei a orientação principal do repositório para registrar novos trabalhos em `/docs/diario/registros2.md`, evitando crescimento adicional de `registros1.md`.
- Atualizei a orientação do próximo passo operacional das redes neurais para usar `/docs/diario/proximo-passo-redes2.md`, evitando crescimento adicional de `proximo-passo-redes.md`.
- Criei os novos arquivos de acompanhamento como ponto de continuação para os próximos registros.
- Comandos usados: `rg --files`, `git status --short`, `cat AGENTS.md`, `find docs/diario -maxdepth 1 -type f -print`, `tail -40 docs/diario/registros1.md` e edição via script Python local.

## 2026-07-10 — Execução do próximo passo antigo das redes neurais
- Acessei `docs/diario/proximo-passo-redes.md` e confirmei o próximo passo operacional mais recente: não treinar nova TCN; primeiro implementar/testar melhoria estrutural de dataset/target para eventos extremos de neutralidade, com regra de abstenção para `neutral` extremo.
- Implementei a guarda configurável `apply_neutral_extreme_event_filter`, aplicada após a guarda de regime/liquidez e antes do stop de drawdown intrafold, para neutralizar decisões direcionais quando features point-in-time indicarem evento extremo (`abs(return_5d)`, `financial_volume_z20`, `volume_ratio_20d` e/ou `volatility_20d`).
- Propaguei os hiperparâmetros `neutral_event_min_abs_return_5d`, `neutral_event_min_financial_volume_z20`, `neutral_event_min_volume_ratio_20d` e `neutral_event_min_volatility_20d` no endpoint de treino, orquestrador, hash de família e sufixo de versão (`nev_<hash>`), mantendo rastreabilidade da política.
- Atualizei testes unitários e de função para cobrir neutralização de eventos extremos, aplicação na economia MUEN, persistência no registry e propagação via candidatos Fase 4.
- Comandos usados: `tail -80 docs/diario/proximo-passo-redes.md`, `rg`, `sed`, edição via script Python local, `python -m black sisacao8/neural_training.py functions/neural_training/sisacao8/neural_training.py functions/neural_training/main.py sisacao8/neural_evolution.py functions/neural_evolution_orchestrator/sisacao8/neural_evolution.py tests/test_neural_training.py tests/test_neural_training_function.py tests/test_neural_evolution.py` e `python -m pytest tests/test_neural_training.py tests/test_neural_evolution.py tests/test_neural_training_function.py -q`.
## 2026-07-10 — Dry-run da guarda de evento neutro extremo
- Executei o primeiro dry-run pós-implementação da guarda `neutral_event_min_*` no endpoint publicado `neural_evolution_orchestrator`, usando família tabular interpretável `tabular_bottleneck_mlp` e thresholds calibrados do caso RCSL3 (`neutral_event_min_abs_return_5d=0.18`, `neutral_event_min_financial_volume_z20=3.5`, `neutral_event_min_volume_ratio_20d=7.5`, `neutral_event_min_volatility_20d=0.05`).
- A chamada retornou HTTP 200 com `status=ok`, `dry_run=true`, `candidate_count=1`, snapshot `neural_eod_training_dataset_2026-07-08_6c894390` e candidata `neural_eod_nev_guard_20260710_tabular_bottleneck_mlp_p50_m08_t35_nev_b4f5a5_01`; portanto a função publicada já reconhece a política e o `model_version` contém o sufixo rastreável `nev_<hash>`.
- Como o dry-run produtivo não grava nem expõe o `training_request` completo na resposta HTTP, não executei treino real ainda. O próximo passo é fazer uma validação pré-treino limitada que confirme, por configuração persistida ou chamada controlada, que os quatro thresholds chegam ao `training_request` antes de liberar execução real.
- Comandos usados: criação de `/tmp/nev_dry_run.json` com heredoc local e `curl -sS -w '\nHTTP_STATUS:%{http_code}\n' -X POST 'https://us-east1-ingestaokraken.cloudfunctions.net/neural_evolution_orchestrator' -H 'Content-Type: application/json' --data @/tmp/nev_dry_run.json`.

## 2026-07-10 — Validação do `training_request` e shadow pequena NEV
- Validei o próximo ponto de parada criando uma candidata com `train_candidates=false`; a configuração persistida em `neural_candidate_configs.training_request_json` confirmou propagação dos quatro thresholds da política `neutral_event_min_*`: `0.18`, `3.5`, `7.5` e `0.05`, além do `candidate_family_hash=neural_eod_phase3_tabular_bottleneck_mlp_p50_m08_t35_nev_rcsl3`.
- Consultei o BigQuery via MCP (`bigquery_query`) e confirmei que nenhuma rede neural está apta para produção no Gate MUEN: `neural_gate_decisions` tem `876` decisões, `0` aprovadas e `876` rejeitadas; `neural_model_registry` contém apenas modelos com `status=candidate`.
- Executei a primeira shadow pequena real da família tabular NEV `p50/m08/t35`; a função retornou HTTP 200, `trained_count=1`, `failed_count=0`, `fold_metric_count=4`, `daily_return_count=3600` e `gate_decision_count=1`, mas o Gate MUEN rejeitou a família por `trades_insuficientes`, `folds_positivos_insuficientes`, `nao_supera_champion_mediana` e `seeds_instaveis` (`total_trades=10`, `positive_folds=0`, `median_delta=0.0`, `stable_across_seeds=false`).
- Para ajudar a buscar redes melhores sem promover nada, executei uma variação shadow um pouco menos restritiva `p48/m05/t50` mantendo a mesma guarda NEV. A cobertura melhorou para `total_trades=36`, mas o Gate MUEN também rejeitou por `folds_positivos_insuficientes`, `nao_supera_champion_mediana` e `seeds_instaveis` (`positive_folds=0`, `median_delta=-0.009692307692307705`, `max_drawdown=0.19564300000000015`).
- Conclusão operacional: ainda não há rede neural pronta para teste em produção. O caminho mais promissor é continuar pesquisa em shadow, mantendo a guarda de evento neutro extremo, mas melhorar o dataset/target e a geração de sinais antes de testar novas arquiteturas recorrentes; o problema atual é falta de edge/folds positivos, não falha técnica de execução.
- Comandos usados: `curl` para `neural_evolution_orchestrator` com payloads `/tmp/nev_config_only.json`, `/tmp/nev_shadow_small.json` e `/tmp/nev_shadow_p48m05.json`; `curl` JSON-RPC HTTP para o MCP `http://mcpserversisacao.shop/mcp`; ferramenta MCP `bigquery_query` para consultar `neural_candidate_configs`, `neural_gate_decisions` e `neural_model_registry`.

## 2026-07-10 — Diagnóstico NEV por folds/tickers e primeira família aprovada no Gate
- Executei o próximo passo operacional: diagnóstico em `neural_fold_metrics` e `neural_daily_returns` para as famílias NEV `p50/m08/t35` e `p48/m05/t50`. A análise confirmou cobertura muito baixa por fold; no `p48/m05/t50`, os maiores detratores por ticker foram `ONCO3` (`delta_return=-0.42`, 6 trades), `VVEO3` (`-0.14`, 2 trades) e `AMBP3` (`-0.14`, 2 trades), enquanto `PCAR3`, `RCSL3` e `RAIZ4` contribuíram positivamente.
- Com base nessa causa confirmada, executei uma shadow real `p48/m05/t50 + NEV` bloqueando `ONCO3`, `VVEO3` e `AMBP3`. A rodada de 1 seed melhorou `median_delta` para `0.01261978384427366` e `positive_folds=2`, mas ainda foi rejeitada por `folds_positivos_insuficientes` e `seeds_instaveis`.
- Em seguida, executei uma rodada shadow pequena com 3 candidatas/seeds/variações controladas para a mesma política bloqueada: `neural_eod_phase3_tabular_bottleneck_mlp_p48_m05_t50_block3_nev_rcsl3_grid3`. A execução foi técnica e operacionalmente bem-sucedida (`trained_count=3`, `failed_count=0`, `daily_return_count=10800`, `fold_metric_count=12`, `gate_decision_count=1`).
- O Gate MUEN aprovou a família `neural_eod_phase3_tabular_bottleneck_mlp_p48_m05_t50_block3_nev_rcsl3_grid3`: `passed=true`, `decision_status=passed`, `seeds=3`, `stable_across_seeds=true`, `positive_folds=8`, `total_trades=30`, `median_delta=0.018891465677179982`, `max_drawdown=0.195643` e `failed_criteria=[]`.
- Mesmo com o Gate aprovado, os três modelos permanecem no registry como `status=candidate`; não houve promoção automática, Scheduler ou uso em produção. A família agora está apta para revisão/validação operacional controlada, não para ativação produtiva automática.
- Comandos usados: `curl` para `neural_evolution_orchestrator` com payloads `/tmp/nev_shadow_p48m05_block3.json` e `/tmp/nev_shadow_p48m05_block3_grid3.json`; `curl` JSON-RPC HTTP para MCP; `bigquery_query` em `neural_fold_metrics`, `neural_daily_returns`, `neural_gate_decisions` e `neural_model_registry`.

## 2026-07-10 — Aprovação manual do champion neural NEV
- Atendendo à aprovação explícita solicitada pelo usuário, executei o fluxo governado `neural_champion_approval` para o melhor candidato individual da família aprovada no Gate MUEN, selecionado por maior `score_total` entre os três modelos da rodada `grid3`: `neural_eod_nev_shadow_p48m05_block3_grid3_20260710_tabular_bottleneck_mlp_p48_m05_t50_bt3_798488_nev_b4f5a5_seed20290717_03`.
- Primeiro executei `approve_if_passed` em `dry_run=true` com `decision_id=gate_f5731a86e7bdf41656987e46b7224780`, `approved_by=user_manual_request` e `approval_ticket=manual-approval-20260710-nev-block3-grid3`; a função retornou HTTP 200, `approved=true`, `failed_checks=[]`, `warnings=[]` e `update_allowed=true`.
- Em seguida executei `approve_if_passed` efetivo com `dry_run=false`; a função retornou HTTP 200 e atualizou o `neural_model_registry.status` do modelo de `candidate` para `approved`, preservando a nota auditável com `decision_id`, aprovador e ticket.
- Validei a aprovação com `audit_current_champion`, que retornou `approved_count=1` e sem warnings, e confirmei no BigQuery que o modelo está com `status=approved`.
- Comandos usados: `rg`/`sed` para localizar o fluxo de aprovação, `curl` para `https://us-east1-ingestaokraken.cloudfunctions.net/neural_champion_approval` nos modos `approve_if_passed` e `audit_current_champion`, e MCP HTTP/JSON-RPC em `http://mcpserversisacao.shop/mcp` com `bigquery_query` para validar `neural_gate_decisions`, `neural_candidate_evaluations` e `neural_model_registry`.

## 2026-07-10 — Tela dedicada do champion NEV e tentativa de schedulers
- Implementei um item de menu dedicado `Champion NEV` no grupo de redes neurais para acompanhar exclusivamente o modelo campeão aprovado, seus metadados de aprovação, decisão Gate MUEN, predições EOD recentes e sinais EOD recentes, sem misturar esse acompanhamento com as telas gerais de treinamento/evolução.
- Adicionei o endpoint backend `GET /ops/neural/champion-monitoring`, que consulta o BigQuery para localizar o modelo neural `status=approved`, recuperar a decisão Gate MUEN associada, buscar a última data de `neural_eod_predictions` do champion e buscar os últimos sinais em `sinais_eod` para o mesmo `model_version` (incluindo o formato `neural:<model_version>`).
- No frontend, criei o hook de consulta com atualização a cada 5 minutos e a aba operacional com cartões de status, métricas do Gate, alerta para tickers bloqueados (`ONCO3`, `VVEO3`, `AMBP3`), tabela de predições e tabela de sinais.
- Tentei criar os schedulers `neural-eod-predictions-daily` e `neural-eod-signals-daily` via MCP/JSON-RPC HTTP usando `cloud_scheduler_job_write`. O primeiro usou `schedule=10 18 * * 1-5`, `time_zone=America/Sao_Paulo`, método `POST`, URI `https://us-east1-ingestaokraken.cloudfunctions.net/neural_eod_predictions` e corpo `{"force":false}`; o segundo usou `schedule=20 18 * * 1-5`, URI `https://us-east1-ingestaokraken.cloudfunctions.net/eod_signals` e corpo `{"signal_source":"neural","force":false}`. As duas chamadas confirmaram a limitação operacional: `PERMISSION_DENIED` por ausência de `cloudscheduler.jobs.create` para `codex-openai@ingestaokraken.iam.gserviceaccount.com`; portanto nenhum scheduler foi criado nesta execução.
- Mantive no próximo passo a criação manual/por operador autorizado dos schedulers necessários: `neural-eod-predictions-daily` às 18:10 BRT e `neural-eod-signals-daily` às 18:20 BRT com `signal_source=neural`; o scheduler `neural-champion-audit-daily` já existe e deve continuar habilitado.
- Comandos usados: `git status --short`, `find .. -name AGENTS.md -print`, `rg`, `sed`, `npm --prefix frontend/app run build`, `npm --prefix frontend/app run lint`, `(cd backend/sisacao-backend && ./mvnw test)`, `python -m flake8`, `python -m pytest tests/test_neural_training.py tests/test_neural_evolution.py tests/test_neural_training_function.py -q`, e `curl` JSON-RPC HTTP para `http://mcpserversisacao.shop/mcp` com `tools/list`, `gcloud_research` e `cloud_scheduler_job_write`.

## 2026-07-10 — Nome fantasia mitológico do champion NEV
- Atendendo ao pedido do usuário, defini o nome fantasia `Apolo NEV` para a rede campeã aprovada, por associação mitológica com profecia, clareza e precisão, mantendo o nome técnico completo (`model_version`) visível na tela para rastreabilidade operacional.
- Atualizei o contrato backend do monitoramento do champion para expor `fantasyName` e `fantasyNameOrigin`, e ajustei a aba `Champion NEV` para mostrar o nome fantasia como título principal e o nome técnico em seguida.
- Comandos usados: `git status --short`, `sed`, edição via script Python local, `npm --prefix frontend/app run lint` e `(cd backend/sisacao-backend && ./mvnw test)`.

## 2026-07-11 — Diagnóstico de ausência de predições/sinais no Champion NEV
- Investiguei a tela `Champion NEV` após questionamento do usuário sobre a ausência de sinais para o próximo pregão. A API produtiva `GET http://34.194.252.70/api/ops/neural/champion-monitoring` confirmou que existe champion aprovado (`status=approved`) e Gate MUEN `passed`, mas os arrays retornados para `predictions` e `signals` estão vazios.
- A causa operacional confirmada é que ainda não há materialização pós-aprovação do fluxo EOD neural: a tela só exibe o que já foi gravado em `neural_eod_predictions` para o `model_version` aprovado e em `sinais_eod` com `model_version` neural. Como os schedulers `neural-eod-predictions-daily` e `neural-eod-signals-daily` não foram criados na etapa anterior por `PERMISSION_DENIED` de Cloud Scheduler, nada disparou automaticamente a inferência e a geração de sinais depois da aprovação manual do champion.
- Também tentei usar o MCP obrigatório por HTTP/JSON-RPC (`http://mcpserversisacao.shop/mcp`) para consultar BigQuery/logs, mas o endpoint alternou entre `503 upstream connect error` e timeout após `initialize`; por isso usei a API backend produtiva como confirmação observável da tela. Não troquei para HTTPS.
- Interpretação para o usuário: sim, deveriam aparecer sinais para o próximo pregão somente depois que `neural_eod_predictions` rodasse para a última data de referência e depois que `eod_signals` rodasse com `signal_source=neural`. No momento do diagnóstico, a aprovação do champion ocorreu, mas o pipeline EOD neural ainda não foi acionado/materializado.
- Próxima correção operacional: executar manualmente, ou criar com uma conta autorizada, `neural-eod-predictions-daily` às 18:10 BRT e `neural-eod-signals-daily` às 18:20 BRT; para antecipar, chamar manualmente `neural_eod_predictions` com `force=true` para a última data de pregão válida e depois `eod_signals` com `signal_source=neural` e `force=true`.
- Comandos usados: `curl -sS 'http://34.194.252.70/api/ops/neural/champion-monitoring' | python3 -m json.tool`, `curl`/Python `requests` para `http://mcpserversisacao.shop/mcp` com `initialize`, tentativa de `tools/list` e tentativa de `tools/call bigquery_query`, `rg`, `sed` e `tail`.

## 2026-07-11 — Comandos para criação dos schedulers EOD neurais
- Atendendo ao pedido do usuário, preparei os comandos `gcloud scheduler jobs create http` para criar os dois schedulers necessários ao fluxo EOD neural do champion: `neural-eod-predictions-daily` às 18:10 BRT e `neural-eod-signals-daily` às 18:20 BRT.
- Mantive os comandos sem OIDC, conforme orientação operacional para funções públicas, e incluí `--location=us-east1`, `--time-zone=America/Sao_Paulo`, método `POST`, headers JSON e payloads mínimos (`{"force":false}` para predições e `{"signal_source":"neural","force":false}` para sinais).
- Comandos usados: `git status --short`, `tail`, `cat` e edição via heredoc local.

## 2026-07-11 — Verificação dos schedulers EOD neurais criados
- Verifiquei via MCP HTTP/JSON-RPC, usando a ferramenta `gcloud_research`, que `neural-eod-signals-daily` existe em `ingestaokraken/us-east1`, está `ENABLED`, agenda `20 18 * * 1-5`, timezone `America/Sao_Paulo`, método `POST`, URI `https://us-east1-ingestaokraken.cloudfunctions.net/eod_signals` e body base64 `eyJzaWduYWxfc291cmNlIjoibmV1cmFsIiwiZm9yY2UiOmZhbHNlfQ==` (`{"signal_source":"neural","force":false}`).
- Também verifiquei que `neural-eod-predictions-daily` existe em `ingestaokraken/us-east1`, está `ENABLED`, agenda `10 18 * * 1-5`, timezone `America/Sao_Paulo`, método `POST`, URI `https://us-east1-ingestaokraken.cloudfunctions.net/neural_eod_predictions` e body base64 `eyJmb3JjZSI6ZmFsc2V9` (`{"force":false}`).
- Os dois jobs estão sem bloco OIDC/OAuth no `httpTarget`, compatíveis com a orientação de usar funções públicas sem OIDC. O próximo disparo agendado reportado para segunda-feira, 2026-07-13, é 21:10 UTC/18:10 BRT para predições e 21:20 UTC/18:20 BRT para sinais.
- Observação operacional: o MCP continua intermitente; houve respostas `503 upstream connect error` antes das leituras bem-sucedidas. Mantive o endpoint obrigatório em HTTP (`http://mcpserversisacao.shop/mcp`) e não usei HTTPS.
- Comandos usados: Python `requests` para `initialize` e `tools/call` no MCP HTTP/JSON-RPC; `gcloud_research` com args `scheduler jobs describe neural-eod-signals-daily --project=ingestaokraken --location=us-east1 --format=json` e `scheduler jobs describe neural-eod-predictions-daily --project=ingestaokraken --location=us-east1 --format=json`.

## 2026-07-11 — Revisão do horário dos schedulers neurais versus disponibilidade de dados
- Respondi à dúvida do usuário sobre se às 18:10/18:20 BRT já existem todos os dados necessários para a rede processar. Pelo código, `neural_eod_predictions` carrega candles da tabela diária `cotacao_ohlcv_diario` até `reference_date`, e `eod_signals` também exige candles diários da mesma tabela para `date_ref`; portanto a dependência crítica é a ingestão diária oficial, não apenas o intraday.
- O manual operacional do projeto agenda `get_stock_data` às 20:00 BRT para gravar `cotacao_ohlcv_diario`; assim, 18:10/18:20 BRT não é um horário seguro/garantido para o fluxo neural EOD se depender da tabela diária oficial do mesmo pregão. Nesse horário a rede pode processar dados do pregão anterior ou retornar vazio se o `date_ref` esperado ainda não estiver disponível.
- Consultei o BigQuery via MCP HTTP/JSON-RPC e confirmei que, no momento da verificação, a tabela diária já continha `max_data_pregao=2026-07-10` com 150 linhas; isso confirma disponibilidade atual, mas não prova que a disponibilidade ocorra sempre antes de 18:10 BRT.
- Recomendação operacional: ajustar os schedulers neurais para rodarem depois do `get_stock_data`, por exemplo `neural-eod-predictions-daily` às 20:10 BRT e `neural-eod-signals-daily` às 20:20/20:30 BRT, ou adicionar uma guarda de frescor que só processe quando `cotacao_ohlcv_diario` tiver linhas para o último pregão.
- Comandos usados: `sed`/`rg` para revisar `docs/manual_agendamentos_gcp.md`, `functions/neural_eod_predictions/main.py` e `functions/eod_signals/main.py`; Python `requests` para MCP HTTP/JSON-RPC `bigquery_query` consultando `MAX(data_pregao)` e contagem de linhas em `cotacao_ohlcv_diario`.
- Tentei antecipar a correção operacional via `cloud_scheduler_job_write` para alterar `neural-eod-predictions-daily` para `10 20 * * 1-5` e `neural-eod-signals-daily` para `20 20 * * 1-5`, mas as chamadas não concluíram por intermitência do MCP (`503 upstream connect error`). Portanto a recomendação de ajuste pós-20:00 ficou registrada, mas não confirmei alteração remota dos jobs nesta execução.

## 2026-07-11 — Solicitação para mover schedulers neurais para 23h ou depois
- O usuário pediu para colocar os schedulers neurais às 23h ou depois. Atualizei o próximo passo operacional para recomendar `neural-eod-predictions-daily` às 23:10 BRT e `neural-eod-signals-daily` às 23:20 BRT, reduzindo o risco de rodar antes da ingestão diária oficial e dando margem após `get_stock_data` das 20:00 BRT.
- Tentei aplicar a alteração via MCP HTTP/JSON-RPC com `cloud_scheduler_job_write`, mas o runtime autenticado como `codex-openai@ingestaokraken.iam.gserviceaccount.com` retornou `PERMISSION_DENIED` por falta de `cloudscheduler.jobs.update` nos dois jobs. Portanto, a alteração remota ainda precisa ser executada por uma conta autorizada.
- Comandos equivalentes para operador autorizado: `gcloud scheduler jobs update http neural-eod-predictions-daily --project=ingestaokraken --location=us-east1 --schedule='10 23 * * 1-5' --time-zone='America/Sao_Paulo' --update-headers='Content-Type=application/json' ...` e `gcloud scheduler jobs update http neural-eod-signals-daily --project=ingestaokraken --location=us-east1 --schedule='20 23 * * 1-5' --time-zone='America/Sao_Paulo' --update-headers='Content-Type=application/json' ...`. Observação: em `gcloud scheduler jobs update http`, usar `--update-headers`; `--headers` é aceito em `create`, mas não em `update`.
- Comandos usados: Python `requests` para MCP HTTP/JSON-RPC `initialize` e `tools/call`/`cloud_scheduler_job_write`; edição local de `docs/diario/proximo-passo-redes2.md` e deste diário.


## 2026-07-11 — Correção da flag de headers no update do Cloud Scheduler
- O usuário testou o comando de atualização para `neural-eod-predictions-daily` e o `gcloud` retornou `unrecognized arguments: --headers=Content-Type=application/json`. Corrigi a orientação: para `gcloud scheduler jobs update http`, a flag correta é `--update-headers='Content-Type=application/json'`; `--headers` deve ficar apenas nos comandos `create`.
- Comando corrigido para predições: `gcloud scheduler jobs update http neural-eod-predictions-daily --project=ingestaokraken --location=us-east1 --schedule='10 23 * * 1-5' --time-zone='America/Sao_Paulo' --uri='https://us-east1-ingestaokraken.cloudfunctions.net/neural_eod_predictions' --http-method=POST --update-headers='Content-Type=application/json' --message-body='{"force":false}'`.
- Comando corrigido para sinais: `gcloud scheduler jobs update http neural-eod-signals-daily --project=ingestaokraken --location=us-east1 --schedule='20 23 * * 1-5' --time-zone='America/Sao_Paulo' --uri='https://us-east1-ingestaokraken.cloudfunctions.net/eod_signals' --http-method=POST --update-headers='Content-Type=application/json' --message-body='{"signal_source":"neural","force":false}'`.
- Comandos usados: edição local via Python e `git diff --check`.

## 2026-07-11 — Teste manual dos endpoints EOD neurais para ontem
- Respondi à dúvida do usuário testando os endpoints para `date_ref=2026-07-10` com `force=true`. A chamada para `https://us-east1-ingestaokraken.cloudfunctions.net/neural_eod_predictions` retornou HTTP 404, confirmando que a função de predições neurais não está publicada nesse endpoint; portanto, chamar os endpoints agora não gera corretamente os sinais de ontem.
- A chamada para `https://us-east1-ingestaokraken.cloudfunctions.net/eod_signals` com `signal_source=neural`, `force=true` e `date_ref=2026-07-10` retornou HTTP 200, mas `status=empty` com motivo `Sem predições neurais disponíveis para 2026-07-10`, o que confirma a dependência: sem `neural_eod_predictions` materializada, `eod_signals` não gera sinais neurais.
- Verifiquei via MCP `gcloud_research` que `gcloud functions describe neural_eod_predictions --gen2` retorna 404 e que não há Cloud Run service `neural-eod-predictions`; já `eod_signals` existe e está ativo. A causa operacional imediata é ausência de deploy da função de predição neural.
- Corrigi o workflow `.github/workflows/deploy.yml` para incluir `neural_eod_predictions` no matrix de deploy, com memória/timeout maiores e `NEURAL_MODEL_STATUSES=approved`. Também ajustei `functions/neural_eod_predictions/main.py` para, quando `NEURAL_MODEL_ID` não estiver definido, selecionar o registry aprovado mais recente independentemente do `model_id`, priorizando `status=approved`; isso evita ficar preso ao default antigo `neural_eod_mlp` enquanto o champion aprovado atual é tabular bottleneck.
- Próximo passo: publicar o workflow/deploy da função `neural_eod_predictions`; depois rodar `neural_eod_predictions` para `2026-07-10` e só então rodar `eod_signals` neural para a mesma data.
- Comandos usados: `curl` nos dois endpoints produtivos com `force=true`, MCP HTTP/JSON-RPC `gcloud_research` para `functions describe`/`run services describe`, `rg`, `sed`, edição via `apply_patch` e `python -m black functions/neural_eod_predictions/main.py`.

## 2026-07-11 — Correção do healthcheck no deploy de `neural_eod_predictions`
- O deploy de `neural_eod_predictions` falhou com `Container Healthcheck failed`. Reproduzi localmente o empacotamento da função entrando em `functions/neural_eod_predictions` e executando `python -c "import main"`; a falha confirmou `ModuleNotFoundError: No module named 'sisacao8'`.
- A causa confirmada é que o source da função continha apenas `main.py` e `requirements.txt`, enquanto `main.py` importa `sisacao8.neural_inference`. No deploy Gen2, isso faz o container falhar no startup antes de abrir a porta 8080, aparecendo como erro genérico de healthcheck.
- Corrigi `.github/workflows/deploy.yml` para, durante o deploy de `neural_eod_predictions`, vendorizar um pacote mínimo `sisacao8` dentro de `functions/neural_eod_predictions` antes de calcular o fingerprint e empacotar: `neural_inference.py`, `neural_dataset.py`, `neural_training.py`, `neural_muen.py` e `trade_engine.py`, com `__init__.py` mínimo para evitar imports amplos do pacote raiz.
- Validei em diretório temporário que o pacote resultante importa `main` com sucesso (`import ok`).
- Comandos usados: `cd functions/neural_eod_predictions && python -c "import main"`, cópia temporária com `mktemp`, `cp` dos módulos vendorizados, `python -c "import main; print('import ok')"`, edição via `apply_patch` e `git diff --check`.

## 2026-07-11 — Tentativa de gerar sinais do Apolo e correção de dependência BigQuery pandas
- Tentei chamar o modelo Apolo para gerar predições de `date_ref=2026-07-10` com `force=true` e `job_run_id=manual-apolo-20260710-20260711`. O endpoint `neural_eod_predictions` agora respondeu, mas retornou HTTP 500; portanto os sinais do próximo pregão ainda não foram gerados.
- Consultei logs via MCP HTTP/JSON-RPC com `cloud_run_function_logs` e confirmei a causa do HTTP 500: `ValueError: Please install the 'db-dtypes' package to use this function`, disparado por `client.query(...).to_dataframe()` em `_load_candles`. Adicionei `db-dtypes` ao `functions/neural_eod_predictions/requirements.txt`.
- Os logs também mostraram aviso anterior na leitura de feriados: `Unrecognized name: data`. Validei o schema de `feriados_b3` via BigQuery (`data_feriado`, `nome_feriado`, `mercado`, `ativo`, `atualizado_em`) e corrigi `_load_holidays` para consultar `data_feriado` com `COALESCE(ativo, TRUE)`.
- Após essas correções, a função precisa ser redeployada. Só depois repetir a sequência: chamar `neural_eod_predictions` para `2026-07-10`; se retornar `rows > 0`, chamar `eod_signals` com `signal_source=neural` para a mesma data.
- Comandos usados: `curl` para `neural_eod_predictions`, MCP HTTP/JSON-RPC `cloud_run_function_logs`, MCP `bigquery_query` em `INFORMATION_SCHEMA.COLUMNS` de `feriados_b3`, edição local de `requirements.txt` e `functions/neural_eod_predictions/main.py`, `python -m black`, `python -m py_compile`, teste de import em pacote temporário vendorizado e `git diff --check`.

## 2026-07-11 — Segunda tentativa do Apolo e serialização JSON para BigQuery
- Após o deploy, tentei novamente gerar predições do Apolo para `date_ref=2026-07-10` com `job_run_id=manual-apolo-20260710-20260711-r2`; o endpoint respondeu HTTP 500 e os sinais ainda não foram gerados.
- Consultei logs via MCP `cloud_run_function_logs` e confirmei nova causa: `TypeError: Object of type date is not JSON serializable` durante `insert_rows_json` em `_insert_predictions`. A inferência chegou até a etapa de inserção, mas as colunas `reference_date`/`valid_for` estavam como objetos `date` Python.
- Corrigi `functions/neural_eod_predictions/main.py` para normalizar os registros antes de `insert_rows_json`, convertendo `date`/`datetime` para ISO string, valores nulos pandas para `None` e escalares numpy via `.item()`.
- Próximo passo: redeployar `neural_eod_predictions` novamente e repetir a chamada do Apolo; se `rows > 0`, chamar `eod_signals` neural para `2026-07-10`.
- Comandos usados: `curl` para `neural_eod_predictions`, MCP HTTP/JSON-RPC `cloud_run_function_logs`, edição via `apply_patch`, `python -m black`, `python -m py_compile`, teste local de `_json_ready` e `git diff --check`.

## 2026-07-11 — Apolo processado para o próximo pregão sem sinais acionáveis
- Após novo deploy, chamei `neural_eod_predictions` para `date_ref=2026-07-10` com `job_run_id=manual-apolo-20260710-20260711-r3`. A função retornou HTTP 200, `status=ok`, `rows=150`, `valid_for=2026-07-13`, `model_id=neural_eod_tabular_bottleneck_mlp` e o `model_version` aprovado do Apolo NEV.
- Em seguida chamei `eod_signals` com `signal_source=neural`, `force=true` e `date_ref=2026-07-10`. A função retornou HTTP 200, mas `generated=0`, `stored=0`, `signals=[]`, `valid_for=2026-07-13` e `model_version=neural:<model_version_do_Apolo>`.
- Validei a tela/API `GET http://34.194.252.70/api/ops/neural/champion-monitoring`: agora há 100 predições recentes exibidas para o champion e 0 sinais. As maiores confidências retornadas pela API estão como `HOLD` (por exemplo FRAS3, ONCO3, MGLU3, AMBP3), abaixo do threshold direcional operacional; portanto o resultado correto desta rodada é não emitir BUY/SELL para o próximo pregão.
- Conclusão operacional: o Apolo processou o pregão de 2026-07-10 e materializou predições para o próximo pregão, 2026-07-13, mas não gerou sinais acionáveis porque as predições ficaram neutras/HOLD sob a regra atual de confiança. Não houve falha técnica nesta etapa; houve abstenção do modelo.
- Comandos usados: `curl` para `neural_eod_predictions`, `curl` para `eod_signals`, `curl` para `http://34.194.252.70/api/ops/neural/champion-monitoring` e parse local com Python.

## 2026-07-11 — Reexecução pós-deploy do Apolo
- Após o usuário informar novo deploy, reexecutei `neural_eod_predictions` para `date_ref=2026-07-10` com `job_run_id=manual-apolo-20260710-20260711-r4`. A função retornou HTTP 200, `status=ok`, `rows=150`, `valid_for=2026-07-13`, `model_id=neural_eod_tabular_bottleneck_mlp` e o `model_version` aprovado do Apolo NEV.
- Reexecutei `eod_signals` com `signal_source=neural`, `force=true`, `date_ref=2026-07-10` e `reason=manual-apolo-20260710-20260711-r4`. A função retornou HTTP 200, mas novamente `generated=0`, `stored=0` e `signals=[]`.
- Conclusão operacional mantida: o pipeline técnico está funcionando e materializa predições para o próximo pregão (`2026-07-13`), mas o Apolo permaneceu sem sinais acionáveis sob os thresholds atuais; a saída operacional correta continua sendo abstenção/HOLD, não erro.
- Comandos usados: `curl` para `neural_eod_predictions` e `curl` para `eod_signals`.

## 2026-07-11 — Mensagem de abstenção no frontend do Champion NEV
- Atendendo ao pedido do usuário, adicionei na aba `Champion NEV` um alerta informativo quando existem predições recentes do champion, mas nenhum sinal operacional foi gravado. A mensagem explica que houve abstenção operacional, mostra a data de referência e validade das predições e diferencia o caso em que todas ficaram `HOLD`/neutral do caso em que predições direcionais foram filtradas antes de virar sinal.
- Essa mensagem cobre o caso observado do Apolo para `date_ref=2026-07-10` e `valid_for=2026-07-13`: o pipeline gerou predições, mas `eod_signals` gravou 0 sinais porque não houve BUY/SELL acionável sob os thresholds atuais.
- Comandos usados: edição via `apply_patch`, `npm --prefix frontend/app run lint` e `npm --prefix frontend/app run build`.

## 2026-07-12 — Próximo passo após aprovação do Apolo NEV
- O usuário perguntou se, agora que há um modelo aprovado, o melhor próximo passo é observar o champion atual e simultaneamente buscar um modelo melhor. A recomendação operacional é sim: manter o Apolo NEV em observação controlada/shadow por pelo menos 5 pregões, sem capital real, enquanto a evolução neural continua buscando challengers melhores contra o Apolo como novo benchmark.
- Critérios de observação sugeridos: confirmar execução diária de `neural_eod_predictions` e `eod_signals`, taxa de abstenção/HOLD, quantidade de sinais BUY/SELL quando surgirem, ausência de sinais em tickers bloqueados, comparação das predições com retornos realizados, incidentes de DQ e estabilidade operacional dos schedulers.
- Critérios para buscar challenger: não afrouxar thresholds apenas para produzir sinais; priorizar melhoria real de edge/calibração, redução de abstenção improdutiva, robustez por folds/seeds e comparação MUEN contra o champion aprovado. Qualquer substituição do champion deve continuar manual/governada via Gate MUEN e aprovação explícita.
- Comandos usados: `cat`/heredoc para registro no diário.

## 2026-07-12 — Estratégia para buscar rede melhor que o Apolo
- O usuário perguntou se a busca por uma rede melhor que o Apolo precisa ser feita manualmente aqui ou se podemos criar rotina automatizada. A recomendação é automatizar a busca de challengers em shadow, mantendo governança manual para promoção de champion.
- Proposta operacional: criar/ajustar uma rotina recorrente de evolução neural fora do horário crítico de mercado para gerar poucos candidatos por rodada, treinar, avaliar pelo Gate MUEN contra o Apolo como benchmark, persistir resultados e apenas alertar quando um challenger passar. A rotina não deve executar `approve_if_passed` automaticamente nem trocar o champion sem autorização explícita.
- Controles recomendados: orçamento diário/semanal limitado, `max_trials` baixo, seeds múltiplas apenas quando a primeira evidência justificar, bloqueio dos tickers problemáticos já identificados, comparação obrigatória contra o champion aprovado, registro de taxa de abstenção e validação por pelo menos 5 pregões antes de qualquer uso com capital real.
- Comandos usados: registro em diário via heredoc local.
- Implementação aplicada no código: adicionei a estratégia `apolo_challenger_shadow` ao `neural_evolution_orchestrator`, com espaço de busca controlado para challengers tabulares/wide-deep, tickers bloqueados, guarda NEV e orçamento baixo. Também adicionei teste de dry-run garantindo que a estratégia gera candidatos governados sem promoção automática.

## 2026-07-12 — Comando para alterar Scheduler da Trilha B
- Atendendo ao pedido do usuário, preparei o comando `gcloud scheduler jobs update http` para apontar o Scheduler recorrente de evolução neural para a estratégia `apolo_challenger_shadow`, com cron `30 2 * * 2-6` em `America/Sao_Paulo`, endpoint `neural_evolution_orchestrator`, método `POST`, `--update-headers='Content-Type=application/json'`, payload com `max_trials=1` e sem OIDC enquanto a função permanecer pública.
- Reforcei que, se o operador preferir manter o Scheduler atual de evolução intacto, deve criar um job separado; mas para "alterar o scheduler" o comando base usa `neural-evolution-daily` como alvo.
- Comandos usados: registro em diário via heredoc local e `git diff --check`.

## 2026-07-12 — Como acompanhar a Trilha B pelo backend
- Respondi à dúvida operacional sobre acompanhamento da rotina `apolo_challenger_shadow` pelo backend. Documentei no próximo passo operacional que, no estado atual, o acompanhamento deve combinar os endpoints publicados `/api/ops/neural/champion-monitoring`, `/api/ops/neural/evolution/leaderboard`, `/api/ops/neural/gate-decisions` e `/api/ops/neural/training-runs`.
- Validei os endpoints produtivos disponíveis: `champion-monitoring` retornou o payload do Apolo, `gate-decisions` retornou lista com 984 decisões, `evolution/leaderboard` retornou 100 entradas, e `training-runs` apresentou timeout nesta tentativa pontual, devendo ser reconsultado quando a API/BigQuery estiver responsiva.
- Também registrei comandos `curl` com filtros Python para localizar candidatas da estratégia `apolo_challenger_shadow` no leaderboard e nas decisões MUEN, além da melhoria futura recomendada: criar endpoint backend dedicado `/ops/neural/challenger-shadow` agregando champion, rodadas, candidatos, decisões e deltas contra o Apolo.
- Comandos usados: `rg` para localizar endpoints no backend e docs; `sed`/`tail` para leitura de `OpsController`, `OpsService`, `BigQueryOpsClient` e diários; Python `urllib.request` para consultar `http://34.194.252.70/api/ops/neural/champion-monitoring`, `/training-runs`, `/gate-decisions` e `/evolution/leaderboard`; heredoc para atualizar documentação.

## 2026-07-12 — Correção: acompanhamento da Trilha B pelo frontend
- O usuário corrigiu a pergunta anterior: queria saber como acompanhar a Trilha B pelo frontend, não pelo backend. Atualizei o próximo passo operacional com o roteiro de uso da interface publicada em `http://34.194.252.70/`.
- Roteiro documentado: menu lateral `Redes neurais` → `Champion NEV` para baseline Apolo e abstenção; `Evolução` para `Últimas tentativas MUEN`, famílias e leaderboard; `Treinos` para verificar artefatos criados/treinados e gráfico de redes criadas x testadas; `Visão geral` para leitura executiva.
- Também registrei a ordem prática: clicar `Atualizar`, abrir `Evolução`, procurar decisões MUEN recentes após o Scheduler, validar se alguma veio aprovada, auditar em `Treinos` e comparar com `Champion NEV` antes de qualquer decisão manual.
- Comandos usados: `rg` para localizar abas/hooks no frontend; `sed` para revisar `App.tsx`, `NeuralEvolutionTab.tsx`, `NeuralTrainingRunsTab.tsx` e `NeuralChampionTab.tsx`; heredoc para atualizar documentação.

## 2026-07-12 — Correção flake8 E501 na estratégia Apolo challenger
- O usuário informou falha do CI em `python -m flake8` por `E501 line too long` nas linhas 103 e 122 de `functions/neural_evolution_orchestrator/main.py`, causada pelos hashes longos das famílias `apolo_challenger_shadow`.
- Corrigi os dois valores `candidate_family_hash` quebrando as strings em expressão parentizada, preservando exatamente os mesmos identificadores operacionais e evitando alterar o espaço de busca.
- Comandos usados: `nl -ba functions/neural_evolution_orchestrator/main.py | sed -n '80,135p'`, edição via `apply_patch`, `python -m flake8 functions/neural_evolution_orchestrator/main.py`, `python -m flake8`, `python -m pytest tests/test_neural_evolution_orchestrator_function.py::test_orchestrator_apolo_challenger_shadow_dry_run_is_governed -q` e `git diff --check`.

## 2026-07-14 — Verificação de redes próximas/melhores que o Apolo
- O usuário perguntou se há alguma rede já analisada com performance próxima ou melhor que o Apolo. Consultei os endpoints produtivos do backend (`champion-monitoring`, `evolution/leaderboard`, `gate-decisions` e tentativa de `training-runs`) para comparar o champion aprovado com as últimas decisões MUEN.
- Resultado: o Apolo NEV continua sendo a única rede com `passed=true` entre 984 decisões MUEN retornadas pelo endpoint; não há outra rede aprovada para substituir o champion neste momento.
- Existem candidatas com métrica econômica pontual superior em `median_delta_expectancy_vs_champion`, principalmente `neural_eod_mlp_evo2_20260709_diversity_01` (delta ~0,0324), mas foram rejeitadas por critérios de governança como `drawdown_excessivo` e/ou `seeds_instaveis`; portanto são promissoras para investigação, não aprovadas para promoção.
- A tentativa de consultar `training-runs` pelo backend expirou nesta rodada, então a conclusão operacional foi baseada principalmente em `gate-decisions`, `leaderboard` e `champion-monitoring`.
- Comandos usados: Python `urllib.request` para consultar `http://34.194.252.70/api/ops/neural/champion-monitoring`, `/evolution/leaderboard`, `/gate-decisions` e `/training-runs`, além de parse local JSON para ordenar decisões por `medianDeltaExpectancyVsChampion`.

## 2026-07-14 — Verificação de sinal do Apolo para amanhã
- O usuário perguntou se o Apolo gerou algum sinal para amanhã. Consultei o endpoint produtivo `GET http://34.194.252.70/api/ops/neural/champion-monitoring`.
- Resultado observado: o payload retornou 100 predições recentes do Apolo com `referenceDate=2026-07-13` e `validFor=2026-07-14`, criadas em `2026-07-14T02:10:42Z`; a lista `signals` veio vazia (`0` sinais).
- Conclusão: não há sinal operacional BUY/SELL gerado para amanhã (`2026-07-15`) no endpoint monitorado. O dado mais recente disponível está válido para hoje (`2026-07-14`) e também não contém sinais gravados.
- Comandos usados: Python `urllib.request` para consultar `champion-monitoring` e parse local do JSON para contar `predictions`, `signals`, `referenceDate`, `validFor`, `confidence` e tickers de maior confiança.

## 2026-07-14 — Causa da falta de sinal do Apolo
- O usuário perguntou se a falta de sinal ocorreu por falta de dados. Reconsultei `GET http://34.194.252.70/api/ops/neural/champion-monitoring` e contei o payload de predições/sinais.
- Resultado: não foi falta de dados de inferência. O backend retornou 100 predições do Apolo para `referenceDate=2026-07-13` e `validFor=2026-07-14`, mas todas com `suggestedAction=HOLD`; a lista de sinais operacionais permaneceu vazia.
- As maiores confidências observadas continuaram abaixo do limiar direcional de 0,60 usado pela inferência/sinalização: ONCO3 `0,5279`, AMOB3 `0,4482`, FRAS3 `0,4343`, MGLU3 `0,4282`, GFSA3 `0,4261`.
- Conclusão: a ausência de sinal foi causada por abstenção/filtro de confiança direcional (`HOLD`), não por ausência de predições. Para confirmar dados brutos de candles/dataset além do payload do backend, uma próxima auditoria pode consultar BigQuery via MCP, mas o monitoramento operacional já mostrou que a inferência produziu linhas.
- Comandos usados: Python `urllib.request` para `champion-monitoring` e parse local com `Counter` para `predictions`, `signals`, `referenceDate`, `validFor`, `suggestedAction` e maiores `confidence`.

## 2026-07-15 — Verificação de novas redes aprovadas como Apolo
- O usuário perguntou se houve mais alguma rede aprovada assim como o Apolo. Verifiquei primeiro o endpoint produtivo `GET http://34.194.252.70/api/ops/neural/gate-decisions`, que retornou 985 decisões MUEN no total e apenas 1 com `passed=true`.
- Confirmei a contagem diretamente no BigQuery via MCP HTTP/JSON-RPC (`bigquery_query`) na tabela `ingestaokraken.cotacao_intraday.neural_gate_decisions`: `total_decisions=985` e `passed_decisions=1`.
- A única decisão aprovada continua sendo `neural_eod_phase3_tabular_bottleneck_mlp_p48_m05_t50_block3_nev_rcsl3_grid3`, decidida em `2026-07-10 16:03:26`, com `median_delta_expectancy_vs_champion=0,018891465677179982`, `max_drawdown=0,195643`, `total_trades=30` e `stable_across_seeds=true`.
- A decisão mais recente retornada pelo BigQuery foi `neural_eod_apolo_challenger_wide_deep_p48_m06_t45_nev_block3`, em `2026-07-14 05:31:06`, mas ela foi rejeitada por `folds_positivos_insuficientes`, `drawdown_excessivo` e `seeds_instaveis`.
- Conclusão operacional: até esta verificação, não há nova rede aprovada além do Apolo; a Trilha B gerou pelo menos um challenger recente, mas ele não passou no Gate MUEN.
- Comandos usados: Python `urllib.request` para `/api/ops/neural/gate-decisions`; MCP HTTP/JSON-RPC por HTTP para `initialize`, `tools/list` e `tools/call` com `bigquery_query` em `neural_gate_decisions`.

## 2026-07-15 — Esclarecimento sobre a rede aprovada
- O usuário perguntou se a única aprovação encontrada era do Apolo ou de outra rede. Reconsultei `GET http://34.194.252.70/api/ops/neural/champion-monitoring`.
- Resultado: o monitoramento retornou `fantasyName=Apolo NEV`, `modelId=neural_eod_tabular_bottleneck_mlp`, `status=approved` e `modelVersion=neural_eod_nev_shadow_p48m05_block3_grid3_20260710_tabular_bottleneck_mlp_p48_m05_t50_bt3_798488_nev_b4f5a5_seed20290717_03`.
- A decisão de Gate vinculada ao champion retornou `candidateFamilyHash=neural_eod_phase3_tabular_bottleneck_mlp_p48_m05_t50_block3_nev_rcsl3_grid3`, `passed=true`, `decisionStatus=passed` e `decidedAt=2026-07-10T16:03:26.188852Z`; o mesmo hash aparece em `metrics_json.muen_economics.candidate_family_hash` do champion.
- Conclusão: a única rede aprovada nas 985 decisões é o próprio Apolo NEV; não é uma segunda rede diferente.
- Comandos usados: Python `urllib.request` para `/api/ops/neural/champion-monitoring` e parse local de `champion`, `gateDecision` e `metricsJson`.

## 2026-07-15 — Estratégia: mudar estrutura ou melhorar candidatas promissoras
- O usuário perguntou se vale mais a pena mudar a estrutura de criação das redes ou pegar as de melhor resultado e melhorá-las. Revisei o estado operacional já confirmado: Apolo segue como única aprovação; challengers recentes e candidatos com delta promissor falharam principalmente por `drawdown_excessivo`, `seeds_instaveis`, folds positivos insuficientes e/ou trades insuficientes.
- Recomendação: não fazer uma troca ampla de estrutura agora. O caminho de melhor custo/benefício é explorar de forma direcionada as famílias que já mostraram sinal econômico, reduzindo risco e instabilidade antes de criar arquiteturas totalmente novas.
- Plano sugerido: criar uma trilha de refinamento das melhores famílias (`neural_eod_apolo_challenger_wide_deep_p48_m06_t45_nev_block3`, `neural_eod_mlp_evo2_20260709_diversity_01` e variações tabular bottleneck próximas ao Apolo), variando poucos parâmetros por vez: aumentar exigência de margem/confiança, reduzir `max_trades_per_fold`, bloquear tickers que puxaram drawdown, testar 3+ seeds e comparar contra Apolo no mesmo Gate MUEN.
- Mudança estrutural maior deve ficar como segunda etapa, só se as famílias promissoras continuarem falhando por vários ciclos mesmo após controle de drawdown/seeds; nesse caso, priorizar mudanças pequenas de features/label/calibração antes de arquiteturas radicalmente novas.
- Comandos usados: revisão local com `nl -ba functions/neural_evolution_orchestrator/main.py` para ver o espaço atual `apolo_challenger_shadow` e `nl -ba sisacao8/neural_evolution.py` para comparar espaços de evolução existentes.

## 2026-07-15 — Execução da sugestão de refinamento dos challengers do Apolo
- Atendendo ao pedido do usuário para executar a sugestão, implementei no `neural_evolution_orchestrator` a nova estratégia `apolo_challenger_refinement`, sem substituir o champion e sem promoção automática.
- A estratégia adiciona uma trilha curta e controlada de refinamento para famílias que já mostraram potencial: `wide_deep_mlp`, `tabular_bottleneck_mlp` e uma variação `mlp` inspirada na família diversity. O foco é reduzir risco/instabilidade: `min_directional_probability` entre 0,50 e 0,54, margem entre 0,08 e 0,09, `max_trades_per_fold` entre 25 e 35, `max_fold_drawdown_stop` entre 0,14 e 0,16, e bloqueio ampliado para `ONCO3`, `VVEO3`, `AMBP3`, `GFSA3` e `MGLU3`.
- A nova estratégia usa `seed_repeats_only=True` no gerador Phase 3 para repetir a mesma política controlada com seeds diferentes, de modo que a próxima avaliação foque estabilidade por seeds/folds em vez de abrir uma arquitetura ampla demais.
- Adicionei teste de dry-run garantindo que `apolo_challenger_refinement` gera exatamente as famílias esperadas e preserva os controles de risco: tickers bloqueados, limite de drawdown por fold, limite de trades e hash `apolo_refine`.
- Próximo passo operacional: se o operador quiser rodar em produção, chamar/schedular o `neural_evolution_orchestrator` com `strategy=apolo_challenger_refinement`, `max_trials=3`, `train_candidates=true`, e manter promoção manual somente após `passed=true` no Gate MUEN.
- Comandos usados: edição via `apply_patch`; `python -m black functions/neural_evolution_orchestrator/main.py tests/test_neural_evolution_orchestrator_function.py`; `python -m flake8 functions/neural_evolution_orchestrator/main.py tests/test_neural_evolution_orchestrator_function.py`; `python -m pytest tests/test_neural_evolution_orchestrator_function.py::test_orchestrator_apolo_challenger_refinement_is_risk_controlled tests/test_neural_evolution_orchestrator_function.py::test_orchestrator_apolo_challenger_shadow_dry_run_is_governed -q`; `python -m flake8`; `python -m pytest -q`.

## 2026-07-15 — Necessidade de deploy e Scheduler para evolução natural do refinamento
- O usuário perguntou se a nova estratégia vai evoluir naturalmente ou se ainda precisa alterar algo. Confirmei via MCP HTTP/JSON-RPC (`cloud_scheduler_job`) que o job `neural-evolution-daily` está `ENABLED`, com cron `30 2 * * 2-6`, mas o payload atual ainda usa `strategy=apolo_challenger_shadow`, `max_trials=1`, `random_seed=20260712` e `reason=scheduled-apolo-challenger-shadow`.
- Conclusão: a evolução natural existente continuará rodando a Trilha B antiga (`apolo_challenger_shadow`). A nova trilha `apolo_challenger_refinement` só passará a evoluir automaticamente depois de duas condições: o código ser deployado na Cloud Function `neural_evolution_orchestrator` e o Scheduler ser alterado para enviar `strategy=apolo_challenger_refinement`.
- Não identifiquei necessidade de nova mudança estrutural no código neste momento. A alteração operacional necessária é trocar/criar Scheduler com payload do refinamento; alternativamente, manter o Scheduler atual e executar o refinamento manualmente para uma primeira rodada controlada.
- Comandos usados: MCP HTTP/JSON-RPC por HTTP para `initialize` e `tools/call` com `cloud_scheduler_job` usando `job_name=neural-evolution-daily`, `project=ingestaokraken`, `location=us-east1`; tentativa com argumento `job` retornou `job_name vazio`, então a consulta válida usou `job_name`.

## 2026-07-15 — Verificação de sinais do Apolo no dia
- O usuário perguntou se o Apolo não gerou sinais hoje. Verifiquei `GET http://34.194.252.70/api/ops/neural/champion-monitoring`, BigQuery via MCP HTTP/JSON-RPC e logs das funções `neural_eod_predictions` e `eod_signals`.
- Resultado no monitoramento: `signals=0`; as predições mais recentes ainda são `referenceDate=2026-07-13`, `validFor=2026-07-14`, 100 linhas expostas pela API, todas `suggestedAction=HOLD`.
- Resultado no BigQuery: para `valid_for >= 2026-07-14`, há somente predições com `valid_for=2026-07-14`: 150 linhas, `directional_count=0`, `hold_count=150`, `latest_created_at=2026-07-14 02:10:42.093521+00`; não apareceu linha de sinal neural em `sinais_eod` para esse intervalo.
- O Scheduler `neural-eod-predictions-daily` executou em `2026-07-15T02:10:05Z` e o Scheduler `neural-eod-signals-daily` executou em `2026-07-15T02:20:04Z`, ambos retornando HTTP 200, mas os logs recentes de `eod_signals` registraram `Sem candles disponíveis para 2026-07-14`.
- Conclusão operacional: o Apolo não gerou sinais hoje. Além de não haver BUY/SELL nas últimas predições válidas, a rodada de sinais de hoje não encontrou candles disponíveis para `2026-07-14`; o próximo diagnóstico deve confirmar a carga de candles diários de `2026-07-14` antes de reexecutar predições/sinais.
- Comandos usados: Python `urllib.request` para `champion-monitoring`; MCP HTTP/JSON-RPC `bigquery_query` em `neural_eod_predictions`, `sinais_eod` e `INFORMATION_SCHEMA.COLUMNS`; MCP `gcloud_research` para listar schedulers; MCP `cloud_run_function_logs` para `neural_eod_predictions` e `eod_signals`; `date -u`.

## 2026-07-15 — Diagnóstico via MCP da ausência de sinais do Apolo
- Atendendo ao pedido do usuário, aprofundei a investigação usando o MCP Server por HTTP/JSON-RPC. Consultei schemas e contagens no BigQuery, logs de Cloud Run Functions e jobs do Cloud Scheduler.
- Causa confirmada: não há candles diários em `cotacao_ohlcv_diario` para `data_pregao=2026-07-14`. A tabela diária tem 150 tickers em `2026-07-10` e 150 em `2026-07-13`, mas não retornou linha para `2026-07-14`; já `cotacao_b3` intraday tem 783 linhas/47 tickers em `2026-07-14`, então houve dado intraday parcial, mas não o candle diário oficial/consolidado necessário para as funções EOD.
- Os logs de `get_stock_data` mostram a causa da falha de carga diária: em `2026-07-15 00:50 UTC`, a função tentou baixar `COTAHIST_D14072026.ZIP` da B3 e recebeu HTTP 404; tentou datas anteriores, encontrou arquivos de `2026-07-13`, `2026-07-10` e `2026-07-09`, mas rejeitou em modo estrito porque o alvo era `2026-07-14`; em seguida registrou `Nenhum dado oficial retornado da B3` e `Fallback offline desabilitado pelo pipeline_config`.
- Confirmei em `pipeline_config` que `allow_offline_fallback=false`. Portanto, o pipeline não preencheu `cotacao_ohlcv_diario` para `2026-07-14`, e os logs de `eod_signals` confirmaram `Sem candles disponíveis para 2026-07-14` nas execuções de `2026-07-15 01:00` e `02:20 UTC`.
- Impacto nas redes: sem candle diário de `2026-07-14`, não houve novas predições para `valid_for=2026-07-15`; no BigQuery, as últimas predições continuam em `valid_for=2026-07-14` com 150 `HOLD` e 0 direcionais, e não há sinais neurais para `2026-07-15`.
- Conclusão operacional: a ausência de sinal do Apolo hoje foi causada por bloqueio de dados/candles diários, não por uma nova decisão direcional do modelo. A correção deve começar pela carga/consolidação diária de `2026-07-14`; só depois reexecutar `neural_eod_predictions` e `eod_signals` para esse `date_ref`.
- Comandos usados: MCP `bigquery_query` em `INFORMATION_SCHEMA.COLUMNS`, `cotacao_ohlcv_diario`, `candles_diarios`, `cotacao_b3`, `neural_eod_predictions`, `sinais_eod` e `pipeline_config`; MCP `cloud_run_function_logs` para `get_stock_data`, `dq_checks`, `neural_eod_predictions` e `eod_signals`; MCP `gcloud_research` para listar schedulers.

## 2026-07-15 — Correção operacional da ausência de candles e reexecução do Apolo
- Após confirmar via MCP que a causa era ausência de `cotacao_ohlcv_diario` para `2026-07-14`, reexecutei a Cloud Function `get_stock_data` com `date_ref=2026-07-14`, `force=true` e motivo `manual-fix-apolo-missing-daily-20260714`. A função retornou HTTP 200 `Success`, indicando que a carga diária foi corrigida após nova tentativa.
- Em seguida reexecutei `neural_eod_predictions` com `date_ref=2026-07-14`, `force=true` e `job_run_id=manual-apolo-20260714-fix-20260715`. A função retornou HTTP 200 `status=ok`, `rows=150`, `valid_for=2026-07-15`, `model_id=neural_eod_tabular_bottleneck_mlp` e o `model_version` aprovado do Apolo NEV.
- Reexecutei `eod_signals` neural com `date_ref=2026-07-14`, `force=true` e o mesmo motivo operacional. A função retornou HTTP 200, `requested=150`, `generated=0`, `stored=0`, `signals=[]` e `valid_for=2026-07-15`.
- Validei o endpoint `champion-monitoring`: agora as predições recentes do Apolo estão em `referenceDate=2026-07-14`, `validFor=2026-07-15`, mas as 100 exibidas pela API vieram `suggestedAction=HOLD`; as maiores confidências foram ONCO3 `0,5502`, BRKM5 `0,4589`, CVCB3 `0,4451`, AMOB3 `0,4272` e CMIN3 `0,4220`, abaixo do limiar direcional de 0,60.
- Conclusão operacional: o bloqueio de dados foi corrigido e o Apolo foi reprocessado para `2026-07-15`; o resultado final continua sem sinais BUY/SELL, agora por abstenção/confiança insuficiente do modelo, não mais por falta de candles.
- Comandos usados: Python `urllib.request` para chamar `get_stock_data`, `neural_eod_predictions`, `eod_signals` e `champion-monitoring`; parse local com `Counter` para datas de predição, `suggestedAction` e maiores confidências.

## 2026-07-15 — Mecanismo automático de recuperação para falhas de candles/predições
- O usuário pediu que o sistema tenha mecanismo para se recuperar desse tipo de falha. Primeiro tentei criar via MCP um Scheduler separado `daily-candles-retry-before-neural` às `23:00 America/Sao_Paulo`, mas o MCP/gcloud retornou `PERMISSION_DENIED` para `cloudscheduler.jobs.create` com a service account `codex-openai@ingestaokraken.iam.gserviceaccount.com`.
- Implementei então recuperação no código das funções: `neural_eod_predictions` agora, antes de inferir, verifica se o DataFrame de candles contém a `reference_date`; se não contiver, chama automaticamente `get_stock_data` via HTTP (`DAILY_CANDLES_RECOVERY_URL`) e recarrega os candles. Se ainda faltar candle, retorna `status=empty`, `reason=missing_daily_candles`, sem inferir com dados defasados.
- Também atualizei `eod_signals`: se a consulta de candles diários vier vazia, a função chama `get_stock_data` e recarrega; se as predições neurais vierem vazias, chama `neural_eod_predictions` e recarrega antes de concluir `empty_neural_predictions`.
- As recuperações são controladas por variáveis de ambiente: `ENABLE_DAILY_CANDLES_RECOVERY` (default `true`), `DAILY_CANDLES_RECOVERY_URL`, `NEURAL_PREDICTIONS_RECOVERY_URL`, `DAILY_CANDLES_RECOVERY_TIMEOUT_SECONDS` e `UPSTREAM_RECOVERY_TIMEOUT_SECONDS`. Isso permite manter o comportamento automático em produção e desabilitar em caso de incidente.
- Adicionei testes cobrindo os helpers de recuperação e a validação de presença da data alvo nos candles.
- Comandos usados: MCP `cloud_scheduler_job_write` para tentativa de criação do Scheduler; edição via `apply_patch`; `python -m black functions/neural_eod_predictions/main.py functions/eod_signals/main.py tests/test_eod_signals.py tests/test_neural_eod_predictions_function.py`; `python -m flake8 functions/neural_eod_predictions/main.py functions/eod_signals/main.py tests/test_eod_signals.py tests/test_neural_eod_predictions_function.py`; `python -m pytest tests/test_eod_signals.py tests/test_neural_eod_predictions_function.py -q`; `python -m flake8`; `python -m pytest -q`.

## 2026-07-15 — Nova verificação de redes aprovadas além do Apolo
- O usuário perguntou novamente se há mais alguma rede aprovada além da Apolo. Consultei o backend produtivo `/api/ops/neural/gate-decisions` e confirmei via MCP HTTP/JSON-RPC com `bigquery_query` em `ingestaokraken.cotacao_intraday.neural_gate_decisions`.
- Resultado atualizado: há 986 decisões MUEN no total e apenas 1 com `passed=true`. A única aprovada continua sendo `neural_eod_phase3_tabular_bottleneck_mlp_p48_m05_t50_block3_nev_rcsl3_grid3`, decidida em `2026-07-10 16:03:26`, com `median_delta_expectancy_vs_champion=0,018891465677179982`, `max_drawdown=0,195643`, `total_trades=30` e `stable_across_seeds=true`; este hash é o Apolo NEV.
- A decisão mais recente, em `2026-07-15 05:31:15`, foi `neural_eod_apolo_challenger_tabular_bt_p46_m05_t50_nev_block3`, mas ela foi rejeitada com `failed_criteria=[seeds_instaveis]`. A anterior, `neural_eod_apolo_challenger_wide_deep_p48_m06_t45_nev_block3`, também permaneceu rejeitada por folds positivos insuficientes, drawdown excessivo e seeds instáveis.
- Conclusão operacional: não há nova rede aprovada além da Apolo; houve mais um challenger analisado em `2026-07-15`, mas ele não passou no Gate MUEN.
- Comandos usados: Python `urllib.request` para `http://34.194.252.70/api/ops/neural/gate-decisions`; MCP HTTP/JSON-RPC `initialize` e `tools/call` com `bigquery_query` para contagem total, lista de aprovadas e últimas decisões em `neural_gate_decisions`.

## 2026-07-15 — Por que só houve uma rede analisada no dia
- O usuário questionou por que houve somente uma rede e se não deveria existir agendamento de hora em hora. Consultei via MCP `gcloud_research` a lista de Cloud Scheduler jobs e via `bigquery_query` a tabela `neural_evolution_runs`.
- Resultado: o job que roda a trilha do Apolo é `neural-evolution-daily`, com cron `30 2 * * 2-6` em `America/Sao_Paulo`, payload `strategy=apolo_challenger_shadow`, `budget.max_trials=1`, `random_seed=20260712`, `train_candidates=true` e `reason=scheduled-apolo-challenger-shadow`. Ele executou em `2026-07-15T05:30:02Z`.
- No BigQuery, em `2026-07-15`, apareceu apenas uma run de evolução: `neural_evolution_20260715_053005_e95a16fc`, `strategy=apolo_challenger_shadow`, `max_trials=1`, `status=completed`. Portanto, a existência de uma única rede analisada é coerente com o Scheduler atual.
- Existe um job `neural-evolution-phase3-30m` com cron `*/30 * * * *`, mas ele não usa a trilha do Apolo: o payload é `strategy=phase3_new_families`, `max_trials=1`, `random_seed=20260629`; além disso, o Scheduler aparece com `status.code=13` nas últimas tentativas. Logo, ele não explica novas aprovações/challengers do Apolo.
- Conclusão: não há hoje agendamento horário da trilha do Apolo/refinamento; para ter evolução hora em hora seria preciso alterar/criar Scheduler específico com `strategy=apolo_challenger_refinement` ou `apolo_challenger_shadow`, cron horário e orçamento controlado. A tentativa anterior de criar Scheduler via MCP falhou por falta de permissão `cloudscheduler.jobs.create`.
- Comandos usados: MCP HTTP/JSON-RPC `gcloud_research` com `scheduler jobs list --project=ingestaokraken --location=us-east1 --format=json`; MCP `bigquery_query` em `neural_evolution_runs` filtrando `DATE(started_at) >= 2026-07-15`.

## 2026-07-15 — Comandos para ativar evolução horária/refinamento
- O usuário pediu os comandos para executar no terminal dele. Preparei uma sequência operacional sem OIDC para funções públicas: configurar projeto/região, disparar deploy do workflow `Deploy`, criar ou atualizar um Scheduler separado `neural-evolution-apolo-refinement-hourly` com `strategy=apolo_challenger_refinement`, `max_trials=1`, `train_candidates=true`, executar uma rodada manual do Scheduler e consultar status/resultados.
- Reforcei que a criação/atualização do Scheduler deve ser feita por uma conta com permissão de Cloud Scheduler, pois a tentativa via MCP com a conta do Codex falhou por `cloudscheduler.jobs.create`.
- Também incluí alternativa de execução manual via `curl` direto no endpoint `neural_evolution_orchestrator` caso o operador queira testar uma rodada antes de deixar o Scheduler horário ativo.
- Comandos documentados na resposta: `gcloud config set project`, `gh workflow run Deploy --ref main`, `gcloud scheduler jobs describe/create/update/run`, `curl` para execução manual e `bq query` para verificar últimas runs/decisões.

## 2026-07-15 — Verificação pós-deploy de geração de novas candidatas
- O usuário informou que fez o deploy e perguntou se estamos gerando novas candidatas. Consultei backend produtivo (`evolution/leaderboard`, `gate-decisions`, `training-runs`) e confirmei via MCP HTTP/JSON-RPC com `bigquery_query` em `neural_evolution_runs`, `neural_candidate_configs` e `neural_gate_decisions` nas últimas 12 horas.
- Resultado: há uma run recente em `2026-07-15 05:30:05`, `evolution_run_id=neural_evolution_20260715_053005_e95a16fc`, `strategy=apolo_challenger_shadow`, `max_trials=1`, `status=completed`, com `candidate_count=1`, `trained_count=1`, `evaluated_count=1` e `gate_decision_count=1`.
- A candidata criada foi `neural_eod_phase3_20260715_tabular_bottleneck_mlp_p46_m05_t50_bt3_798488_nev_b4f5a5_01`, família `neural_eod_apolo_challenger_tabular_bt_p46_m05_t50_nev_block3`, `candidate_source=phase3_family`, `architecture_type=tabular_bottleneck_mlp`.
- O Gate MUEN mais recente rejeitou essa família em `2026-07-15 05:31:15` com `failed_criteria=[seeds_instaveis]`. Portanto, há geração de candidata pela trilha diária antiga, mas ainda não há evidência de execução horária nem da nova `apolo_challenger_refinement`.
- Observação: o endpoint backend `/api/ops/neural/training-runs` retornou HTTP 500 nesta consulta, então a confirmação principal veio diretamente do BigQuery via MCP.
- Comandos usados: Python `urllib.request` para `http://34.194.252.70/api/ops/neural/evolution/leaderboard`, `/gate-decisions` e `/training-runs`; MCP HTTP/JSON-RPC `bigquery_query` em `neural_evolution_runs`, `neural_candidate_configs` e `neural_gate_decisions`.

## 2026-07-15 — Verificação atual do Cloud Scheduler após deploy
- O usuário perguntou se ainda falta alterar algo no Cloud Scheduler. Consultei novamente os jobs via MCP HTTP/JSON-RPC `gcloud_research` com `scheduler jobs list --project=ingestaokraken --location=us-east1 --format=json`.
- Estado confirmado: `neural-eod-predictions-daily` e `neural-eod-signals-daily` continuam ativos nos horários EOD (`23:10` e `23:20 America/Sao_Paulo`) e devem se beneficiar do mecanismo de recuperação após deploy das funções. O `neural-evolution-daily` também continua ativo, mas ainda usa `strategy=apolo_challenger_shadow`, `max_trials=1` e cron `30 2 * * 2-6`.
- Ainda não existe Scheduler específico para `apolo_challenger_refinement`/evolução horária. O job `neural-evolution-phase3-30m` roda a cada 30 minutos, mas usa `strategy=phase3_new_families` e aparece com `status.code=13`, portanto não substitui a trilha horária/refinamento do Apolo.
- Conclusão: para a recuperação de candles/predições, não é obrigatório alterar Scheduler se as funções já foram deployadas; para evolução horária/refinamento do Apolo, ainda falta criar/alterar um Scheduler específico com `strategy=apolo_challenger_refinement`. Opcionalmente, criar um Scheduler separado de retry de `get_stock_data` antes das predições como defesa em profundidade, mas a tentativa via MCP falhou por falta de permissão `cloudscheduler.jobs.create`.
- Comandos usados: MCP HTTP/JSON-RPC `initialize` e `tools/call` com `gcloud_research` para listar os Cloud Scheduler jobs em `ingestaokraken/us-east1`.

## 2026-07-15 — Comando para criar Scheduler do refinamento Apolo
- O usuário pediu um comando para criar/alterar o Scheduler que faz a nova estratégia rodar automaticamente. Preparei comando idempotente em Bash para `neural-evolution-apolo-refinement-hourly`, usando `gcloud scheduler jobs describe` para decidir entre `update http` e `create http`.
- O comando usa função pública sem OIDC, endpoint `https://us-east1-ingestaokraken.cloudfunctions.net/neural_evolution_orchestrator`, cron horário `0 * * * *`, timezone `America/Sao_Paulo`, `attempt-deadline=1800s`, header `Content-Type=application/json` e payload `strategy=apolo_challenger_refinement`, `max_trials=1`, `random_seed=20260715`, `train_candidates=true`.
- Reforcei que o operador precisa executar com conta que tenha permissão de Cloud Scheduler (`cloudscheduler.jobs.create/update`), pois a tentativa via MCP/Codex não tinha essa permissão.

## 2026-07-15 — Verificação do Scheduler horário de refinamento Apolo
- O usuário pediu para verificar se o comando de criação do Scheduler foi executado corretamente. Consultei via MCP HTTP/JSON-RPC o job `neural-evolution-apolo-refinement-hourly` com `cloud_scheduler_job`.
- Resultado do Scheduler: o job existe em `ingestaokraken/us-east1`, está `ENABLED`, usa cron `0 * * * *`, timezone `America/Sao_Paulo`, método `POST`, `attemptDeadline=1800s` e aponta para `https://us-east1-ingestaokraken.cloudfunctions.net/neural_evolution_orchestrator`.
- O corpo configurado decodifica para `strategy=apolo_challenger_refinement`, `budget.max_trials=1`, `budget.random_seed=20260715`, `train_candidates=true` e `reason=scheduled-hourly-apolo-refinement`. Portanto, a criação/configuração do Scheduler está correta.
- Validação adicional: um `dry_run` direto no endpoint produtivo de `neural_evolution_orchestrator` com `strategy=apolo_challenger_refinement`, `max_trials=1`, `random_seed=20260715` e `train_candidates=false` retornou HTTP 200, `status=ok`, `candidate_count=1`, gerando a versão `neural_eod_phase3_20260715_tabular_bottleneck_mlp_p50_m08_t35_d16_bt5_ea0ddc_nev_b4f5a5_01`. Isso confirma que o código deployado reconhece a nova estratégia e consegue montar candidata em modo seco.
- Porém, ainda não há evidência de execução persistida do novo Scheduler: consultas BigQuery nas últimas 6 horas em `neural_evolution_runs`, `neural_candidate_configs` e `neural_gate_decisions` retornaram 0 linhas recentes. O único config com `phase3_20260715` encontrado foi o challenger diário antigo criado às `2026-07-15 05:30:05` pela estratégia `apolo_challenger_shadow`.
- Os logs recentes de `neural_evolution_orchestrator` mostram HTTP 500 em horários de meia em meia hora com `ValueError: No neural evolution candidates were generated`; isso é compatível com o job antigo `neural-evolution-phase3-30m` (`*/30 * * * *`, `strategy=phase3_new_families`), cujo `lastAttemptTime` aparece em `2026-07-15T17:30:01Z`, e não prova falha do novo Scheduler horário.
- Conclusão operacional: o Scheduler novo foi criado corretamente e o deploy suporta a estratégia; falta aguardar/forçar uma tentativa real do job e confirmar se ela grava uma run/candidata no BigQuery. Se após a próxima virada de hora continuar sem `neural_evolution_runs`, executar `gcloud scheduler jobs run neural-evolution-apolo-refinement-hourly --project=ingestaokraken --location=us-east1` e reconsultar BigQuery/logs.
- Comandos usados: MCP HTTP/JSON-RPC `cloud_scheduler_job` para `neural-evolution-apolo-refinement-hourly` e `neural-evolution-phase3-30m`; MCP `cloud_run_function_logs` para `neural_evolution_orchestrator`; MCP `bigquery_query` em `INFORMATION_SCHEMA`, `neural_evolution_runs`, `neural_candidate_configs` e `neural_gate_decisions`; Python `urllib.request` para `dry_run` produtivo do endpoint `neural_evolution_orchestrator`.

## 2026-07-15 — Verificação após primeira execução do Scheduler horário Apolo
- O usuário pediu nova verificação. Consultei via MCP HTTP/JSON-RPC o Scheduler `neural-evolution-apolo-refinement-hourly`, logs de `neural_evolution_orchestrator` e BigQuery nas últimas 2 horas.
- Resultado: o Scheduler horário executou em `2026-07-15T18:00:00.825103Z` e houve POST 200 no `neural_evolution_orchestrator` às `2026-07-15 18:00:00`, confirmando execução real bem-sucedida do refinamento.
- BigQuery confirmou 1 run, 1 config e 1 gate recentes. A run `neural_evolution_20260715_180004_6a223366` iniciou em `2026-07-15 18:00:04`, terminou em `2026-07-15 18:01:27`, `strategy=apolo_challenger_refinement`, `status=completed`, com `candidate_count=1`, `trained_count=1`, `evaluated_count=1`, `gate_decision_count=1` e `failed_count=0`.
- A candidata criada foi `neural_eod_phase3_20260715_tabular_bottleneck_mlp_p50_m08_t35_d16_bt5_ea0ddc_nev_b4f5a5_01`, `candidate_source=phase3_family`, registrada às `2026-07-15 18:00:04`.
- O Gate MUEN decidiu `neural_eod_apolo_refine_tabular_bt_p50_m08_t35_dd16_block5` em `2026-07-15 18:01:14` como `rejected`, `passed=false`, com `failed_criteria=[trades_insuficientes,seeds_instaveis]`. Portanto, o fluxo gerou, treinou e avaliou corretamente, mas a candidata não foi aprovada.
- Observação importante: no mesmo minuto também apareceu POST 500 com `ValueError: No neural evolution candidates were generated`, compatível com a execução concorrente do job antigo `neural-evolution-phase3-30m` no minuto cheio. Esse erro não impediu a run `apolo_challenger_refinement` de concluir com sucesso.
- Conclusão operacional: o Scheduler novo está funcionando. Próximo foco é monitorar as próximas rodadas horárias e investigar/desabilitar ou corrigir separadamente o `neural-evolution-phase3-30m`, pois ele continua gerando ruído de erro 500 sem relação direta com o refinamento do Apolo.
- Comandos usados: MCP HTTP/JSON-RPC `cloud_scheduler_job` para `neural-evolution-apolo-refinement-hourly`; MCP `cloud_run_function_logs` para `neural_evolution_orchestrator`; MCP `bigquery_query` em `neural_evolution_runs`, `neural_candidate_configs` e `neural_gate_decisions`.
