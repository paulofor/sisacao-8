name: Deploy MCP Server C++ (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: C++ workflow disabled
        run: |
          echo "Workflow da versão C++ desativado. Use deploy-mcp-java-vps.yml"
